/* let str = 'london';

let result = str.slice(0, 1).toUpperCase() + str.slice(1);
console.log(result); // выведет 'London' */

/* let str = "london";
let result = str.slice(0, -1) + str.slice(-1).toLocaleUpperCase(2);

console.log(result); */

/* let str = "london";
let result = str.slice(0, 2).toLocaleUpperCase() + str.slice(2);
console.log(result); */

/* let str = 'London';
console.log(str.slice(0,1).toLocaleLowerCase() + str.slice(1)); */

/* let str = "word1 word2 word3";
let world = str.split(" ");
for (let i = 0; i < world.length; i++) {
  world[i] = world[i].slice(0, 1).toLocaleUpperCase() + world[i].slice(1);
}

let join = world.join(" ");
console.log(join); */

/* let str = "var_test_text";
let text = str.split("_");
for (let i = 0; i < text.length; i++) {
  text[i] = text[i].slice(0, 1).toLocaleUpperCase() + text[i].slice(1);
}

let result = text.join("");
console.log(result); */

/* let str = '12345';
let result = str.split('').reverse().join('');

console.log(result);  */

/* let text = 'Dura Pizda';
let result = text.split('').reverse().join('');
console.log(result); */